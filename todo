 (---PASSING---)
new
----


2. Point total calculate????(almost ***)
 *** moon shoot is remaining

1. Next round start (done)
4. game Over(done)
6. if dont have any othe card can start with heart (done).

3. restart the game with after 13 trick. (done)

5. (almost) - test write of all the functions 


 (+++REFACTOR+++)

game.js {



	// this.getValidator=function(trick,step) {
	// 	if(trick==1)
	// 		return new FirstTrickValidator();
	// 	if(trick==2)
	// 		return new SecondTrickValidator();
	// 	return new OtherTrickValidator();
	// };

	
	// var validator = getValidator(trick,step);


	// var FirstTrickValidator = function(){
	// 	this.isValid = function(player,card){

	// 	}
	// }

}